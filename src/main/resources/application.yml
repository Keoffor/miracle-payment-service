server:
  port: 9193

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3307/payment_transactions
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
      dialect: org.hibernate.dialect.MySQLDialect
stripe:
  secret:
    key: {add_your_key}
  webhook:
    secret:
      key:  {add_secret_key}
